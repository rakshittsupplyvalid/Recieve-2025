{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": false,
    "jsx": "react",
    "resolveJsonModule": true,
    "skipLibCheck": true
    // "types": ["node", "jest"] // Uncomment and edit if you need specific types
  }
}